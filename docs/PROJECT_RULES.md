When changing a file:

- regenerate the whole file

Never invent project structure.

Never remove existing constants.

Preserve public APIs where possible.

Use the repository as the source of truth.

Prefer extending existing code over rewriting it.